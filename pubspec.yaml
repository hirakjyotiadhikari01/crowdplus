name: secoundapp
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.1.0  # ✅ For GPS location
  connectivity_plus: ^5.0.2  # ✅ For checking network state (Wi-Fi/Mobile)
  http: ^0.13.5  # ✅ Required for API requests

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/fire.jpg
